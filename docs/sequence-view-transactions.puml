@startuml ViewTransactions_Sequence
!theme plain
skinparam backgroundColor #f3f6fb
skinparam participant {
  BackgroundColor #ffffff
  BorderColor #2b6cb0
  FontColor #0b3954
}
skinparam arrow {
  Color #2b6cb0
}

actor "User" as user
participant "TransactionList\n(View)" as view
participant "TransactionList\n(Controller)" as controller
participant "TransactionService\n(Model)" as service
database "LocalStorage" as storage

user -> view: navigates to /transactions
view -> controller: ngOnInit()
controller -> service: getAll()
service -> storage: localStorage.getItem("ft_transactions_v1")
storage --> service: return JSON array
service --> controller: return sorted transactions[]
controller -> view: displayedColumns & transactions
view --> user: render transaction table

note right of service
  Service sorts transactions
  by date (newest first)
end note

note right of view
  Material table displays:
  Date, Description, Amount, Type, Actions
end note

@enduml
